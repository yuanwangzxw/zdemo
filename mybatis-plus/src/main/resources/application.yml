virtualHost: 192.168.56.101
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    #如果LocalDateTime.now()存入mysql的时间早8个小时的话，就需要在连接处指定时区
    url: jdbc:mysql://${virtualHost}/account?characterEncoding=utf8&useSSL=true&serverTimezone=Hongkong&nullNamePatternMatchesAll=true
    password: 123456
    username: root

mybatis-plus:
  type-aliases-package: com.zxw.mybatis.entity
